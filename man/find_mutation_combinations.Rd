% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategy_helpers.R
\name{find_mutation_combinations}
\alias{find_mutation_combinations}
\title{Select mutation combinations based on different strategies}
\usage{
find_mutation_combinations(
  mutations,
  guides,
  pams,
  maximum_mutations_per_template,
  strategy
)
}
\arguments{
\item{mutations}{GRanges object with all possible mutations.}

\item{guides}{A GRanges object. For \code{strategy = "best_single"} or \code{"all_valid"},
this should be a single guide. For \code{strategy = "best_global"}, this can be
a GRanges object containing multiple guides.}

\item{pams}{A GRanges object, corresponding to the \code{guides}.}

\item{maximum_mutations_per_template}{The number of mutations to select (N).}

\item{strategy}{A character string specifying the selection mode. One of
\code{"best_single"}, \code{"best_global"}, or \code{"all_valid"}.}
}
\value{
For \code{strategy = "best_single"} or \code{"best_global"}, a single list containing
the selected mutations and summary statistics.
For \code{strategy = "all_valid"}, a list of lists, where each inner list represents
a valid combination.
}
\description{
A unified function to find optimal or all combinations of mutations.
It can operate in three modes:
\enumerate{
\item \code{"best_single"}: Greedily finds the best set of non-overlapping mutations
for a \emph{single} guide. Prefers mutations that disrupt the PAM, then the guide.
\item \code{"best_global"}: Greedily finds the best set of non-overlapping mutations
that disrupt the \emph{maximum number} of guides/PAMs from a larger set.
\item \code{"all_valid"}: Finds all possible non-overlapping combinations of a given size
for a \emph{single} guide, filtered to those that overlap the guide or PAM.
}
}
\keyword{internal}
