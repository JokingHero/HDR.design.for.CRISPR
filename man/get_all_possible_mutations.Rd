% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synonymous_snps_helpers.R
\name{get_all_possible_mutations}
\alias{get_all_possible_mutations}
\title{Generate All Possible Single Nucleotide Variants (SNVs)}
\usage{
get_all_possible_mutations(
  search_positions,
  genome,
  alphabet = c("A", "C", "T", "G")
)
}
\arguments{
\item{search_positions}{A \code{GRanges} object from the \code{GenomicRanges} package.
Each range should represent a single genomic coordinate (i.e., width of 1).}

\item{genome}{A \code{BSgenome} object or a \code{DNAStringSet} from which the
reference sequence can be extracted using \code{getSeq()}.}

\item{alphabet}{A character vector representing the set of possible
nucleotides. Defaults to \code{c("A", "C", "T", "G")}. The alternate alleles
are selected from this set.}
}
\value{
A \code{GRanges} object with \code{length(search_positions) * 3} rows (assuming
the default 4-base alphabet). It includes metadata columns:
\item{REF}{The reference allele at the given position.}
\item{ALT}{The alternate (mutant) allele.}
}
\description{
The function returns a \code{GRanges} object where each input position is expanded
to three rows, one for each possible alternate allele. The strand of all
input ranges is standardized to \code{+} to ensure consistent retrieval of the
reference base from the forward strand, mimicking VCF format conventions.
}
\keyword{internal}
